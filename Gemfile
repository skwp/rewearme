source 'https://rubygems.org'

gem 'rails', '3.2.8'
gem 'jquery-rails'
gem 'haml'
gem 'sass'
gem 'compass'
gem 'compass-rails'
gem 'nokogiri'
gem 'htmlentities'
gem 'money'
gem 'money-open-exchange-rates'
gem 'fancybox-rails', :git => 'https://github.com/ding280/fancybox-rails.git'
gem 'mini_magick'
gem 'aws-s3'          # for storing images in S3
gem 'awesome_print'
gem 'randumb' # randomize results

group :development do
  gem 'sqlite3'
end

group :production do
  gem 'pg'
end
# Gems used only for assets and not required
# in production environments by default.
group :assets do
  gem 'sass-rails'
  gem 'coffee-rails'

  # See https://github.com/sstephenson/execjs#readme for more supported runtimes
  # gem 'therubyracer', :platforms => :ruby

  gem 'uglifier', '>= 1.0.3'
end


group :development do
  gem 'rspec-rails'
end

# To use ActiveModel has_secure_password
# gem 'bcrypt-ruby', '~> 3.0.0'

# To use Jbuilder templates for JSON
# gem 'jbuilder'

# Use unicorn as the app server
gem 'unicorn'

# Deploy with Capistrano
# gem 'capistrano'

# To use debugger
# gem 'debugger'
